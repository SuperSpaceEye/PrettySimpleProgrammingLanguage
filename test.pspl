fn int fib(int n) {
    if (less_eq(n, 1)) {
        return(n);
    } endif
    return(add(fib(sub(n, 1)), fib(sub(n, 2))));
}

fn void main() {
    var int a;

    asgn(a, fib(20));

    print_num(a);

    return();
}