fn int fib(int n) {
    #doesn't work
    #if (less_eq(n, 1)) {
    #    return(n);
    #} endif
    #asgn(n, add(fib(sub(n, 1)), fib(sub(n, 2))));
    #return(n);

    print_num(n);

    if (more(n, 1)) {
        var int fib1;
        var int fib2;

        asgn(fib1, fib(sub(n, 1)));
        asgn(fib2, fib(sub(n, 2)));

        asgn(n, add(fib1, fib2));
        #asgn(n, add(fib(sub(n, 1)), fib(sub(n, 2))));
    } endif
    return(n);
}

fn void main() {
    var int a;

    asgn(a, fib(6));
    print_num(a);
    print_num(-10);
    return();
}