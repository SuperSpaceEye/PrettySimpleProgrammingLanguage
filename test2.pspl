fn int test2(int n) {return(n);}

fn int test(int n) {
    var uint a;

    if (less_eq(a, to_uint(1))) {
        print_num(10);
        return(n);
    }

    var int d;
    asgn(n, test(n));
    return(n);
}

fn void main() {
    return();
}